

public class Constants {
    public static void main(String[] args) {
        // a constant must start with the final keyword
        final double KILOGRAMS = 0.454;
        final int MONTHS_IN_A_YEAR = 12;
        final double DOOR_HEIGHT = 15,DOOR_WIDTH = 30; 

        // mathematical functions that can be used in a program
        double stan = Math.sqrt(36);
        double ran = Math.random();
        double sit = Math.pow(ran, ran);
       System.out.println(stan);
       System.out.println(ran);
       System.out.println(sit);
    }
}
